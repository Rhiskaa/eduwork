{
  "name": "this_cy_bdd",
  "version": "1.0.0",
  "main": "index.js",
  "author": "RizkaS",
  "license": "MIT",
  "devDependencies": {
    "@cypress/xpath": "^2.0.3",
    "cypress": "^12.5.1",
    "cypress-xpath": "^2.0.1",
    "prettier": "^2.8.4"
  },
  "dependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^15.1.4",
    "@cypress/browserify-preprocessor": "^3.0.2",
    "cypress": "^13.5.1",
    "cypress-iframe": "^1.0.1"
  },
  "cypress-cucumber-preprocessor": {
    "nonGlobalStepDefinitions": false,
    "stepDefinitions": "**/stepDef/*.js",
    "html": {
      "enabled": true
    }
  },
  "scripts": {
    "cy.open": "cypress open",
    "featureLogin": "cypress run --spec cypress/e2e/features/login.feature",
    "chrome.on.login": "cypress run --spec cypress/e2e/features/login.feature --headed --no-exit --browser chrome"
  }
}
